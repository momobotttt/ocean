{
  "name": "scoreplay-app",
  "version": "1.0.0",
  "description": "ScorePlay — 預測積分遊戲 (points-only, no real money) — frontend + admin backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
